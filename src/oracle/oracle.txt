CREATE TABLE employee(
  id NUMBER(4),
  name VARCHAR2(20),
  gender CHAR(1),
  birth DATE,
  salary NUMBER(6,2),
  job VARCHAR2(30),
  deptno NUMBER(2)
);

DESC table_name;查看表结构
看到表的列的名字，以及对应的类型，长度等

删除一张表的语法
DROP  TABLE table_name;
DROP TABLE employee;